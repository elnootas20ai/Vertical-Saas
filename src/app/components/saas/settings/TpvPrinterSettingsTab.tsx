import { TpvPrinterSetupPanel } from '../TpvPrinterSetupPanel';

/** Ajustes de gerente: misma UI que el modal del TPV. */
export function TpvPrinterSettingsTab() {
  return <TpvPrinterSetupPanel variant="page" />;
}
