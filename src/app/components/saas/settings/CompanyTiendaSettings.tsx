import { SalesPointsTab } from './SalesPointsTab';

/** Tiendas / centros de trabajo + PDV (delivery y compraventa comparten SalesPointsTab). */
export function CompanyTiendaSettings() {
  return <SalesPointsTab />;
}
