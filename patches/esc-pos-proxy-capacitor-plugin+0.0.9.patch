diff --git a/node_modules/esc-pos-proxy-capacitor-plugin/android/src/main/java/com/revagon/place/menu/app/plugins/escpos/ESCPOSProxyPlugin.java b/node_modules/esc-pos-proxy-capacitor-plugin/android/src/main/java/com/revagon/place/menu/app/plugins/escpos/ESCPOSProxyPlugin.java
index 8b91a21..25e9d11 100644
--- a/node_modules/esc-pos-proxy-capacitor-plugin/android/src/main/java/com/revagon/place/menu/app/plugins/escpos/ESCPOSProxyPlugin.java
+++ b/node_modules/esc-pos-proxy-capacitor-plugin/android/src/main/java/com/revagon/place/menu/app/plugins/escpos/ESCPOSProxyPlugin.java
@@ -109,6 +109,22 @@ public class ESCPOSProxyPlugin extends Plugin {
     });
   }
 
+    @PluginMethod
+  public void getLocalNetworkInfo(PluginCall call) {
+    getBridge().execute(() -> {
+      String ip = getLocalIpv4();
+      JSObject ret = new JSObject();
+      if (ip != null && ip.contains(".")) {
+        ret.put("ip", ip);
+        ret.put("prefix", ip.substring(0, ip.lastIndexOf('.')));
+      } else {
+        ret.put("ip", "");
+        ret.put("prefix", "");
+      }
+      call.resolve(ret);
+    });
+  }
+
   @PluginMethod
   public void discover(PluginCall call) {
     JSArray portArray = call.getArray("ports");
@@ -184,7 +200,7 @@ public class ESCPOSProxyPlugin extends Plugin {
       return;
     }
 
-    String[] services = {"_pdl-datastream._tcp.", "_printer._tcp."};
+    String[] services = {"_pdl-datastream._tcp.", "_printer._tcp.", "_epos._tcp.", "_ipp._tcp.", "_jetdirect._tcp."};
     CountDownLatch latch = new CountDownLatch(services.length);
     Handler mainHandler = new Handler(Looper.getMainLooper());
 
diff --git a/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxy.swift b/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxy.swift
index 6501e68..225ed09 100644
--- a/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxy.swift
+++ b/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxy.swift
@@ -7,6 +7,17 @@ import Darwin
 
     @objc public func sendEscPosCommand(ip: String, port: Int, message: Data, completion: @escaping (Bool) -> Void) {
         let connection = NWConnection(host: NWEndpoint.Host(ip), port: NWEndpoint.Port(integerLiteral: NWEndpoint.Port.IntegerLiteralType(port)), using: .tcp)
+        let queue = DispatchQueue(label: "escpos-proxy")
+        var finished = false
+
+        let finish: (Bool) -> Void = { success in
+            if finished {
+                return
+            }
+            finished = true
+            completion(success)
+            connection.cancel()
+        }
 
         connection.stateUpdateHandler = { (newState) in
             switch (newState) {
@@ -15,24 +26,26 @@ import Darwin
                 connection.send(content: message, completion: .contentProcessed({ (error) in
                     if let error = error {
                         print("Failed to send message: \(error)")
-                        completion(false)
+                        finish(false)
                     } else {
                         print("Message sent successfully")
-                        completion(true)
+                        finish(true)
                     }
-                    connection.cancel()
                 }))
             case .failed(let error):
                 print("Failed to connect: \(error)")
-                completion(false)
-                connection.cancel()
+                finish(false)
+            case .cancelled:
+                finish(false)
             default:
                 break
             }
         }
 
-        let queue = DispatchQueue(label: "escpos-proxy")
         connection.start(queue: queue)
+        queue.asyncAfter(deadline: .now() + .seconds(10)) {
+            finish(false)
+        }
     }
 
     @objc public func ping(ip: String, port: Int, completion: @escaping (Bool, NSNumber?) -> Void) {
@@ -70,41 +83,62 @@ import Darwin
 
     @objc public func discoverPrinters(ports: [Int], timeout: Int, completion: @escaping ([[String: Any]]) -> Void) {
         let targetPorts = ports.isEmpty ? [9100, 9101, 9102] : ports
+        let timeoutMs = max(3000, timeout)
         var collected = [[String: Any]]()
         var seen = Set<String>()
         let group = DispatchGroup()
+        var completed = false
+        let lock = NSLock()
+
+        let safeComplete: () -> Void = {
+            lock.lock()
+            defer { lock.unlock() }
+            if completed {
+                return
+            }
+            completed = true
+            let deduped = collected.filter { item in
+                guard let ip = item["ip"] as? String, let port = item["port"] as? Int, let source = item["source"] as? String else {
+                    return false
+                }
+                let key = "\(ip):\(port):\(source)"
+                if seen.contains(key) {
+                    return false
+                }
+                seen.insert(key)
+                return true
+            }
+            completion(deduped)
+        }
 
         group.enter()
         mdnsHelper = MdnsCollector(resolver: { [weak self] data in
             return self?.ipv4String(from: data)
         }, completion: { [weak self] found in
+            lock.lock()
             collected.append(contentsOf: found)
+            lock.unlock()
             self?.mdnsHelper = nil
             group.leave()
         })
-        mdnsHelper?.start(types: ["_pdl-datastream._tcp.", "_printer._tcp."], timeoutMs: timeout)
+        mdnsHelper?.start(types: ["_pdl-datastream._tcp.", "_printer._tcp.", "_epos._tcp.", "_ipp._tcp.", "_jetdirect._tcp."], timeoutMs: timeoutMs)
 
         group.enter()
         DispatchQueue.global(qos: .utility).async { [weak self] in
-            if let scanResults = self?.scanSubnet(ports: targetPorts, timeoutMs: timeout) {
+            if let scanResults = self?.scanSubnet(ports: targetPorts, timeoutMs: timeoutMs) {
+                lock.lock()
                 collected.append(contentsOf: scanResults)
+                lock.unlock()
             }
             group.leave()
         }
 
         group.notify(queue: DispatchQueue.main) {
-            let deduped = collected.filter { item in
-                guard let ip = item["ip"] as? String, let port = item["port"] as? Int, let source = item["source"] as? String else {
-                    return false
-                }
-                let key = "\(ip):\(port):\(source)"
-                if seen.contains(key) {
-                    return false
-                }
-                seen.insert(key)
-                return true
-            }
-            completion(deduped)
+            safeComplete()
+        }
+
+        DispatchQueue.main.asyncAfter(deadline: .now() + .milliseconds(timeoutMs + 1500)) {
+            safeComplete()
         }
     }
 
@@ -113,11 +147,12 @@ import Darwin
             return []
         }
 
-        let semaphore = DispatchSemaphore(value: 20)
+        // 60 conexiones a la vez y ~500 ms por host: un /24 completo cabe en el timeout.
+        let semaphore = DispatchSemaphore(value: 60)
         let group = DispatchGroup()
         var results = [[String: Any]]()
         let lock = NSLock()
-        let perHostTimeout = max(200, min(1000, timeoutMs / 4))
+        let perHostTimeout = max(350, min(900, timeoutMs / 6))
 
         for host in 1...254 {
             let ip = "\(prefix).\(host)"
@@ -139,7 +174,7 @@ import Darwin
             }
         }
 
-        group.wait()
+        _ = group.wait(timeout: .now() + .milliseconds(timeoutMs))
         return results
     }
 
@@ -174,7 +209,7 @@ import Darwin
 
         connection.start(queue: queue)
         queue.asyncAfter(deadline: .now() + .milliseconds(timeoutMs)) {
-            finish(reachable)
+            finish(false)
         }
 
         group.wait()
@@ -188,6 +223,52 @@ import Darwin
         }
         defer { freeifaddrs(addressList) }
 
+        // Preferir SIEMPRE la WiFi (en*/wlan*): la primera interfaz puede ser la de
+        // datos moviles (pdp_ip*) y escanearia otra red donde nunca esta la impresora.
+        var wifiPrefix: String? = nil
+        var fallbackPrefix: String? = nil
+        var pointer = first
+        while true {
+            let interface = pointer.pointee
+            if interface.ifa_addr.pointee.sa_family == UInt8(AF_INET) {
+                let name = String(cString: interface.ifa_name)
+                if name.hasPrefix("en") || name.hasPrefix("pdp_ip") || name.hasPrefix("wlan") {
+                    var hostname = [CChar](repeating: 0, count: Int(NI_MAXHOST))
+                    getnameinfo(interface.ifa_addr, socklen_t(interface.ifa_addr.pointee.sa_len), &hostname, socklen_t(hostname.count), nil, 0, NI_NUMERICHOST)
+                    let ipString = String(cString: hostname)
+                    let components = ipString.split(separator: ".")
+                    if components.count == 4 && !ipString.hasPrefix("169.254.") && !ipString.hasPrefix("127.") {
+                        let prefix = components.dropLast().joined(separator: ".")
+                        if name.hasPrefix("en") || name.hasPrefix("wlan") {
+                            if wifiPrefix == nil {
+                                wifiPrefix = prefix
+                            }
+                        } else if fallbackPrefix == nil {
+                            fallbackPrefix = prefix
+                        }
+                    }
+                }
+            }
+            if let next = interface.ifa_next {
+                pointer = next
+            } else {
+                break
+            }
+        }
+        return wifiPrefix ?? fallbackPrefix
+    }
+
+    @objc public func getLocalNetworkInfo() -> [String: Any] {
+        var addressList: UnsafeMutablePointer<ifaddrs>? = nil
+        guard getifaddrs(&addressList) == 0, let first = addressList else {
+            return ["ip": "", "prefix": ""]
+        }
+        defer { freeifaddrs(addressList) }
+
+        var wifiIp: String? = nil
+        var wifiPrefix: String? = nil
+        var fallbackIp: String? = nil
+        var fallbackPrefix: String? = nil
         var pointer = first
         while true {
             let interface = pointer.pointee
@@ -198,8 +279,17 @@ import Darwin
                     getnameinfo(interface.ifa_addr, socklen_t(interface.ifa_addr.pointee.sa_len), &hostname, socklen_t(hostname.count), nil, 0, NI_NUMERICHOST)
                     let ipString = String(cString: hostname)
                     let components = ipString.split(separator: ".")
-                    if components.count == 4 {
-                        return components.dropLast().joined(separator: ".")
+                    if components.count == 4 && !ipString.hasPrefix("169.254.") && !ipString.hasPrefix("127.") {
+                        let prefix = components.dropLast().joined(separator: ".")
+                        if name.hasPrefix("en") || name.hasPrefix("wlan") {
+                            if wifiIp == nil {
+                                wifiIp = ipString
+                                wifiPrefix = prefix
+                            }
+                        } else if fallbackIp == nil {
+                            fallbackIp = ipString
+                            fallbackPrefix = prefix
+                        }
                     }
                 }
             }
@@ -209,7 +299,7 @@ import Darwin
                 break
             }
         }
-        return nil
+        return ["ip": wifiIp ?? fallbackIp ?? "", "prefix": wifiPrefix ?? fallbackPrefix ?? ""]
     }
 
     private func ipv4String(from data: Data) -> String? {
@@ -237,6 +327,7 @@ import Darwin
         private var browsers: [NetServiceBrowser] = []
         private var services: [NetService] = []
         private let resolver: (Data) -> String?
+        private var completed = false
 
         init(resolver: @escaping (Data) -> String?, completion: @escaping ([[String: Any]]) -> Void) {
             self.resolver = resolver
@@ -250,8 +341,12 @@ import Darwin
                 browser.delegate = self
                 browsers.append(browser)
                 browser.searchForServices(ofType: type, inDomain: "local.")
-                DispatchQueue.main.asyncAfter(deadline: .now() + .milliseconds(timeoutMs)) { [weak browser] in
-                    browser?.stop()
+            }
+            DispatchQueue.main.asyncAfter(deadline: .now() + .milliseconds(timeoutMs)) { [weak self] in
+                guard let self = self else { return }
+                self.browsers.forEach { $0.stop() }
+                DispatchQueue.main.asyncAfter(deadline: .now() + .milliseconds(400)) { [weak self] in
+                    self?.forceComplete()
                 }
             }
         }
@@ -282,13 +377,26 @@ import Darwin
         }
 
         private func finishSearch() {
+            if completed {
+                return
+            }
             if pendingSearches > 0 {
                 pendingSearches -= 1
             }
             if pendingSearches == 0 {
-                completion?(results)
-                completion = nil
+                forceComplete()
+            }
+        }
+
+        private func forceComplete() {
+            if completed {
+                return
             }
+            completed = true
+            pendingSearches = 0
+            browsers.forEach { $0.stop() }
+            completion?(results)
+            completion = nil
         }
     }
 }
diff --git a/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxyPlugin.swift b/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxyPlugin.swift
index d094fef..3b698de 100644
--- a/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxyPlugin.swift
+++ b/node_modules/esc-pos-proxy-capacitor-plugin/ios/Sources/ESCPOSProxyPlugin/ESCPOSProxyPlugin.swift
@@ -8,7 +8,8 @@ public class ESCPOSProxyPlugin: CAPPlugin, CAPBridgedPlugin {
     public let pluginMethods: [CAPPluginMethod] = [
         CAPPluginMethod(name: "print", returnType: CAPPluginReturnPromise),
         CAPPluginMethod(name: "ping", returnType: CAPPluginReturnPromise),
-        CAPPluginMethod(name: "discover", returnType: CAPPluginReturnPromise)
+        CAPPluginMethod(name: "discover", returnType: CAPPluginReturnPromise),
+        CAPPluginMethod(name: "getLocalNetworkInfo", returnType: CAPPluginReturnPromise)
     ]
     private let implementation = ESCPOSProxy()
 
@@ -63,4 +64,9 @@ public class ESCPOSProxyPlugin: CAPPlugin, CAPBridgedPlugin {
             call.resolve(["printers": printers])
         }
     }
+
+    @objc func getLocalNetworkInfo(_ call: CAPPluginCall) {
+        let info = implementation.getLocalNetworkInfo()
+        call.resolve(info)
+    }
 }
\ No newline at end of file
