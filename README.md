# udar

Frontend SaaS (React + Vite). Documentación en `readme/` (estructura: `readme/main.md`, instalación: `readme/install.md`).

**Notas y pendientes varios** (incl. tienda activa delivery / visión gerente): `readme/Untitled` — sección *«Notas de desarrollo — tienda activa en delivery»*.
